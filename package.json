{
  "name": "isapp",
  "private": true,
  "workspaces": [
    "front",
    "back"
  ],
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "rimraf ./dist && cd back && yarn prestart:back:prod && cd ../front && yarn prestart:front:prod && cd ..",
    "s": "yarn soda",
    "size": "cd front && yarn prestart:front:prod && size-limit"
  },
  "dependencies": {
    "tsconfig-paths": "^3.7.0"
  },
  "devDependencies": {
    "@solid-soda/scripts": "^1.10.1",
    "rimraf": "^2.6.2",
    "size-limit": "^1.3.5"
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn soda lint-staged",
      "commit-msg": "yarn soda commitlint"
    }
  },
  "publishConfig": {
    "access": "public"
  },
  "size-limit": [
    {
      "webpack": false,
      "limit": "100 KB",
      "path": [
        "front/public/bundle.js",
        "front/public/bundle.css"
      ]
    }
  ]
}
